import React from 'react';
import {Text,View,StyleSheet} from 'react-native'

export  default App = () => {
 <View style={styles.container
 }>
   <Text style={styles.title}  > Respiratorio</Text>
    </View>

}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#FFF',
  },
  title:{
    fontSize: 20,
    textAlign: 'center',
    margin: 10,
    color: 'white',
  }
});